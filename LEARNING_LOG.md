# JODIE-NAS 项目学习记录

> **目标**:从"代码基本完成"提升到"能独立写论文"的水平。
> **模式**:小步循环——每轮讲一个模块(白话+图示)→ 答 2 个检查题 → 批改 → 下一模块。
> **更新时间**:2026-08-14(第 1 次会话)

---

## 一、入学诊断(2026-08-14)

第一轮六题摸底结果:

| 题 | 主题 | 评分 | 结论 |
|---|---|---|---|
| Q1 | 研究问题 | 50% | 能说"做了什么",说不出"要解决什么问题" |
| Q2 | JODIE 增量 | 15% | 只碰到动机皮毛;把 t-batch 与 TGN 时间窗口批处理混淆 |
| Q3 | BPR loss | 0% | 空白(公式: `L = -log σ(ŷ_pos - ŷ_neg)`) |
| Q4 | REINFORCE 循环 | 40% | 知道采样/训练,不知道奖励来源与策略更新 |
| Q5 | 并行后端 | 65% | 直觉正确,漏掉"跨进程状态重建"这一核心难点 |
| Q6 | 论文贡献 | 65% | 三个合理贡献,但漏掉最有价值的"评估保真度"贡献 |

**诊断结论**:代码层 B-,研究层 D+。起点定为:先建全景地图,再逐模块补方法细节,领域知识穿插讲解。

**核心改判**:ATTEMPTS_LOG 的排障过程(Pipeline 评分偏差根因链)是潜在的核心论文贡献,非"工程事故"。

---

## 二、第 1 课:项目全景地图 ✅

**一句话总结**:
> 整个项目只做一件事:给"一个架构"算出"一个分数",然后想办法让这件事更快、更准。

**数据流骨架**:

```
数据层        模型层         训练/评估层        NAS 层              执行层
CSV ──► Interaction 流 ──► 模型读交互 ──► BPR 训练 ──► 控制器采样架构 ──► 三种后端决定
       按时间切 train/     更新嵌入打分      MRR/Recall      训练评估得奖励       在哪算:
       val/test 分区                     出分数 ◄──────── RL 更新策略         串行 / 数据并行 / 流水线
```

**三个关键直觉**:
1. 时间是一等公民(按时间切分、靠时间更新嵌入)
2. NAS = 循环:采样 → 训练 → 打分 → 引导下次采样
3. 三种后端是同一循环的三种跑法:Serial 慢而准 / DP 较快 / Pipeline 最快但污染评分

---

## 三、第 2 课:数据层(jodie/data/) ✅

**三个概念**:
1. `Interaction` = 项目原子单位,4 字段:`timestamp, user_id, item_id, features`
2. 时间分区:必须按时间切 train/val/test,随机切会导致**时序泄漏**
3. split 标签:分区只是"一段按时间排好的交互 + 标签",train/val/test 由使用者区分

**检查题记录**:

| 题 | 用户回答 | 批改 |
|---|---|---|
| C1 为什么 0.95 掺水? | "时间泄露,未来数据被提前展示" | ✅ 正确。标准术语:时序泄漏(temporal leakage) |
| C2 features 是什么? | "用户爱好、物品性能等特征" | ⚠️ 半对。features 是**交互级特征**(这次交互本身的属性),不是用户/物品的静态属性;后者叫 static embedding,模型层细讲 |

---

## 四、第 3 课:模型层——一次交互如何流过模型 ✅(概念过关)

> 首次用符号讲用户反馈"没听懂",改用**印象卡片类比 + 具体数字**重讲后过关。教训:抽象符号必须配类比和数字。

**印象卡片类比**:
- 每个用户/物品有一张"印象卡片"(向量,概括历史和风格),随每次交互被改写
- 一次交互 (u=5, i=3, t=100) 的四步:
  1. **读卡片**:取出 u5、i3 当前卡片
  2. **互递归改写**:写卡机器(RNN/GRU)读入四样 → [u 旧卡片, i 旧卡片, Δt, features] → 吐出新卡片;u 和 i 的卡片**都被改写**,互为养分
  3. **投影**:把卡片沿"运动趋势"推到预测发生的未来时刻(补偿 t=100→200 之间的漂移)
  4. **打分**:逐位相乘再求和(点积),分数最高的物品 = 预测

**检查题记录**:

| 题 | 用户回答 | 批改 |
|---|---|---|
| C3 为什么必须投影? | "忽略了随时间变化用户和物品本身特征的变化" | ✅ 正确,即卡片在两次更新之间的漂移 |
| C4 谁被改写?输入是什么? | 用户5和物品3的卡片;输入=物品3旧卡片、用户5旧卡片、时间、交互特征 | ✅ 全对;细节:改 i 时用 u 更新**前**的旧卡片 |

---

## 五、第 4 课:训练层之 BPR——模型怎么"学"

(答题待批改后补全)

**背景**:训练数据只有正交互(发生过),没有"负标签"(没发生 ≠ 不喜欢)。
**做法**:每条正交互 (u, i) 随机配一个负样本物品 j(u 没交互过的),要求 ŷ(u,i) > ŷ(u,j)。

**损失公式**:`L = -log σ(ŷ_pos - ŷ_neg)`,σ = sigmoid(把任意数压到 0~1)

**数值直觉**:

| 分数差 ŷ_pos - ŷ_neg | σ(分数差) | 损失 -log σ | 含义 |
|---|---|---|---|
| +2 | 0.88 | 0.13 | 正样本遥遥领先 → 惩罚很小 |
| -2 | 0.12 | 2.1 | 负样本反超 → 惩罚爆炸 |

**为什么不用 pointwise 交叉熵**:隐式反馈没有真负样本,"没交互"≠"不喜欢";BPR 只关心相对顺序,与评估指标(MRR/Recall 都是排序指标)自洽。

**检查题**:
- C5:负样本 j 是怎么来的?如果 j 恰好是 u 真实交互过的物品,会发生什么?
- C6:为什么不直接要求"正样本分数=1、负样本分数=0"?

---

## 六、教学模式升级:说明项目(annotated/) — 2026-08-14

用户要求更细的掌握程度,升级为"带批注的镜像项目":

- 位置:`annotated/`,结构镜像 `jodie/`,每源文件对应一个 .md
- 标注约定:🔑 核心逻辑(贴源码+逐段讲)/ 📖 了解即可 / ⏭️ 跳过 / ❓ 检查题 / 📄 论文关键点
- 阅读顺序见 [annotated/README.md](annotated/README.md),按模块分批生成,检查题批改通过后生成下一批
- 已生成:数据层 3 文件(synthetic / temporal_partition / public_dataset),检查题 D1-D6

**检查题记录(数据层)**:

| 题 | 用户回答要点 | 批改 |
|---|---|---|
| D1 RNG 保存/恢复的作用 | "全局随机数被消耗,后面结果不一样" | ✅ 对;补充:表现为"同样代码两次运行结果不同",极难复现 |
| D2 负样本冻结的目的 | "防止两个策略训练现场抽到的负样本不一致" | ✅ 对;升华:控制变量思想,分数差异只能归因于执行后端 |
| D3 overlap 的 step 计算 | "20条重叠,第80到第100之间" | ⚠️ 半对:重叠数/位置对,漏答 step=80;块0=[0,100),块1=[80,180) |
| D4 partition_id 全局唯一的原因 | "ID重复导致查找错误" | ✅ 对;具体:执行层按 ID 派发任务/记日志,重号不可分辨 |
| D5 ID 重映射 | "[100,200,300]→[0,1,2],按行号查表" | ✅ 对 |
| D6 负样本预生成与 Pipeline | "现场抽会导致两策略数据有差别;仍未解决分数不一致" | ✅ 两问全对,"必要但不充分"理解到位 |

**数据层模块通过 ✅**(2026-08-14)

### 模型层批次(已生成,检查题 E1-E8 待答)

- [annotated/jodie/models/jodie_rnn.md](annotated/jodie/models/jodie_rnn.md) — 重点:buffer vs Parameter、detach 写回、互递归拼装
- [annotated/jodie/models/gnn_encoder.md](annotated/jodie/models/gnn_encoder.md) — 重点:mean/sum/attn 三聚合、时间衰减、注意力融合
- [annotated/jodie/models/hybrid_jodie.md](annotated/jodie/models/hybrid_jodie.md) — 重点:统一内存表、节点 ID 偏移、门控、agg/peer
- [annotated/jodie/models/factory.md](annotated/jodie/models/factory.md) — 重点:config→模型、工厂解耦对 NAS 的意义

| 题 | 状态 |
|---|---|
| E1 buffer vs Parameter | ⏳ 待答 |
| E2 detach 写回的原因 | ⏳ 待答 |
| E3 sum/mean 何时相近 | ⏳ 待答 |
| E4 时间衰减在 attn/sum 中的结合 | ⏳ 待答 |
| E5 节点 ID 偏移 | ⏳ 待答 |
| E6 门控 g=1/g=0 的行为 | ⏳ 待答 |
| E7 字符串开关解析 | ⏳ 待答 |
| E8 工厂解耦的好处 | ⏳ 待答 |

---

## 七、已掌握清单

- [x] 项目全景地图(一句话 + 数据流)
- [x] 数据层:Interaction / 时间分区 / 时序泄漏
- [x] 模型层(概念级):印象卡片 / 互递归改写 / 投影 / 点积打分
- [ ] 训练层:BPR 数学 / t-batch / 评估指标(进行中:第 4 课 BPR)
- [ ] NAS 层:搜索空间 / 控制器 / REINFORCE
- [ ] 执行层:三种后端的原理与 Pipeline 偏差根因
- [ ] 领域层:JODIE 论文 / TGN / NAS 背景
- [ ] 论文层:贡献陈述 / 实验设计 / 章节写作
