# 04 · 双臂消融(2026-08-26 ~ 08-28)

**原始结果在服务器**,分支 `ablation/tbatch`(桥梁臂)、`ablation/stale-batch`(stale 臂);本目录不放原始数据。

**关键数字(全部实锤)记录在仓库根目录 `RAW_ABLATION_GUIDE.md` §7**:

| 臂 | 选中架构 | 各自报告的 test | 真实串行 test |
|---|---|---|---|
| serial 基准 | 133,888(emb128/static-off) | 0.8561 | 0.856121275963994 |
| t-Batch 桥梁 | 133,888(同家族) | 0.8793 | — |
| stale_batch | 147,840(emb64/static-on) | 0.9335 | **0.6014** |

**作用**:段 4 案例的干净归因——唯一变量 = 批处理模式,证明"分批本身无害(t-Batch),不懂依赖的分批才有害(stale)"。评分污染 +0.345(val)、+0.332(test);掉分 -0.2547。

**陷阱教训**:两臂日志写 "Serial training" 但 batch_mode 取自 base_config(各臂自己的模式)——判读落到代码,不落到日志文案。
