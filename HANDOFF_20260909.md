# 会话交接文档(HANDOFF)

> **用途**:在服务器开新对话继承任务时的完整上下文。新会话先读本文件,再读下面的「文件地图」。
> **日期**:2026-09-09
> **分支**:`ablation/positioning`(本文件随该分支推送到 origin,服务器 `git fetch && git checkout ablation/positioning && git pull` 即可拿到)
> **用户**:山东大学研究生,DepTGL 论文作者

---

## 1. 用户画像与工作模式(新会话必读)

- 用户 ML/系统基础较弱,代码系 AI 生成;**问答式教学**:先讲后考,通俗中文 + 类比 + 具体数字,每轮一个小模块,结尾出 **2 个检查题**。
- 目标是最终能自己写论文、给审稿人/导师讲清楚(不只是拿到稿子)。
- 用户拍板的结论不要轻易推翻;有分歧先摆证据让用户裁决。

## 2. 项目目标(一句话)

**DepTGL**:面向时序 GNN(JODIE 家族)的 NAS 框架论文,PVLDB 风格、仿 DynaHB 八段引言。当前阶段:引言已两轮批改、摘要 v4 待批改;实验证据链基本齐(缺 Phase 2 变量隔离与完整实验表)。

## 3. 三大贡献(用户 2026-08-27 拍板,勿改结构)

| 贡献 | 内容 | 定位 | 证据 |
|---|---|---|---|
| C1 | 图神经网络搜索系统(旗舰;保真执行 = 任何后端复现串行搜索的**选择**) | 对(质量) | E1/E2/E3/E4 |
| C2 | Pipeline 流水线策略 | 快(有条件) | E8:判定「部分成立」 |
| C3 | 异步架构生成+训练(3.0× vs naive) | 快 | E5 |

- 论文灵魂:**「快和对天生打架」→ DepTGL 两者兼得**。
- ⚠️ C1 承诺已软化(2026-08-28 二轮批改):只承诺**选择级**复现("reproduces the serial search's selection"),不承诺候选分数级精确相等;最终 test 位级一致(0.856121275963994)是实验章证据,不进承诺。
- ⚠️ t-Batch/stale_batch 不占 intro 结构位(裁决 B);t-Batch 只在 Section 5 以"集成自 JODIE"出现一次;Challenge I 的"现有方法不够"句用泛化写法、不点名不引用。

## 4. 实验证据链总览(截至 2026-09-09)

| 编号 | 实验 | 关键数字 | 状态 |
|---|---|---|---|
| E1 | 四策略对比(serial/DP/naive/smart) | test 全部 0.8561;耗时 25,965 / 15,746 / 16,783 / 11,113s(1.0/1.6/1.5/2.3×) | ✅ 08-13 |
| E2 | 五档消融(f1/f2/f3 逐一开关) | 砍 f1 或 f2 → 选错架构 402K/0.69999;砍 f3 无害(负对照);全开 ×2 位级一致 | ✅ 08-24~25 |
| E3 | 双臂消融(serial/t-Batch/stale) | 分批本身无害(t-Batch 同家族),朴素分批害人(stale 选 147K) | ✅ 08-26~28 |
| E4 | 补测(147K/133K 串行复评) | 147K 真实 test 0.6014(自报 0.9335 是伪装);133K 复评位级一致 | ✅ 08-28 |
| E5 | smart-async 复证 | 3.0× 加速(5,486s vs 16,290s)、test 位级一致、RL 0 崩溃 | ✅ 08-26~27 |
| E6 | 噪声地板(同配置重跑) | mean\|d\|=0.0069 / max\|d\|=0.0887,判读尺子 | ✅ 08-25 |
| E7 | 分区倾斜统计 | 事件数均匀(1.00),倾斜在 new users:20K 1.83× / 100K 4.09× / 全量 114.5× | ✅ 09-03 |
| E8 | 定位实验 Phase 1(B 20K×2GPU、D 100K×3GPU,7/7 run) | **C2 判定「部分成立」**,详见 §5 | ✅ 09-08/09 |
| E9 | Phase 2 变量隔离 | 待用户决定 | ⏳ |
| E10-E12 | 完整实验表 / C3 vs BOHB / 时间分解图 | 阶段 3 设计 | 📋 |

## 5. C2 判定细节(定位实验核心结论)

coarse 阶段 trials/hour(协议:BATCH_MODE=serial、SEED=42、partition 2000、rnn_only):

| 考场 | smart | naive | DP | serial | naive/smart | naive/DP |
|---|---|---|---|---|---|---|
| B: 20K×2GPU | 37.2 | 31.7 | 11.6 | — | 0.85 | 2.73× |
| D: 100K×3GPU | 8.4 | **9.5** | **10.2** | 7.2 | **1.13** | 0.93 |

- **判定「部分成立」**:naive 反超 smart(0.85→1.13 跨 1,「重负载下流水线逼近 trial 并行」成立一半);naive 未反超 DP(2.73→0.93,100K 下 DP 快 7%)。
- **100K 绝对加速比塌缩**:smart 1.17× / naive 1.32× / DP 1.42× vs serial(20K 主配置为 2.3-3.0×)→「没有哪种策略在所有规模都是赢家」= 按 workload 选策略故事的最有力论据。
- **保真度分层**:20K 下 smart ≡ naive 位级一致、test 0.8561 命中 133K 家族锚点;DP 两种规模均偏离家族架构、val 最差(100K:val 0.36 vs naive 0.56)。
- **⚠️ 引用雷区**:08-13 FINAL_REPORT 的 DP 1.6× 是 BATCH_MODE≠serial 的旧协议,不能与 Phase 1 新数字并列引用;论文引用 DP 数字必须注明协议。
- 完整判读:`positioning/RESULTS_20260909.md`;Section 6 素材:`论文/draft_v2_实验结果_C2.md`。

## 6. 文件地图(按用途)

```
POSITIONING_GUIDE.md            ← C2 定位实验运行说明(§0 三结局判读、§2 配置表、§5 判读;时间线已更新)
positioning/RESULTS_20260909.md ← Phase 1 全部原始数据 + 三层判读
positioning/configs.py          ← Phase 1 用过的运行工具链(configs + apply_config + launch + chain_all + precheck)
论文/draft_v2_摘要.md           ← 摘要 v4(待用户批改)
论文/draft_v2_引言.md           ← 八段引言(两轮批改已应用,段 5-8 待用户批改)
论文/draft_v2_实验结果_C2.md    ← Section 6「执行策略选择」小节素材
论文/draft_v1_摘要引言构思.md    ← 构思 + 两轮批改记录(附在第九、十节)
论文/overleaf草稿/main.tex      ← PVLDB 模板组装版(只填摘要+引言;2 个 % TODO 挂账同步点)
论文/overleaf草稿/实验总表.md   ← E1-E13 作战地图(全景 + 人话版详解 + 待跑)
论文/overleaf草稿/实验归档/     ← 01-06 编号成果(04/05/06 是说明.md 指针,原始数据在服务器/分支)
论文/overleaf草稿/待清除/       ← 待确认删除(先不要真删)
```

## 7. 写作铁律与裁决(已由用户拍板)

1. 承诺用词:选择级,不是分数级(摘要 v4 里 "scores and selection" 待同步为 "selection")。
2. 段 4 案例四幕:评分污染(+0.345 val/+0.332 test)→ 选择洗牌(133K→147K)→ 掉分(test 0.6014 vs 0.8561,-0.2547)→ 伪装(自报 0.9335 > 0.8561 > 真实 0.6014)。
3. 数字必须实测,判读落到代码/数据,不落到日志文案(日志写死 "Serial training",以 batch_mode 配置为准)。
4. 协议差异必须注明(BATCH_MODE、SEED、partition 等)。
5. Challenge III 数字:new users 倾斜 "up to 4×"(锚定 100K;若主配置改 20K 锚定则 1.8×);不要再写"interaction counts vary widely"(实测事件数均匀)。
6. DepTGL 全称未确认(猜测 Dependency-aware Temporal Graph Learning),正文用 `\texttt{DepTGL}`。

## 8. 技术事实备忘(新会话踩坑预防)

- 唯一变量协议 = BATCH_MODE=serial;保真三组件 f1 种子纪律 / f2 状态迁移 / f3 负样本预分配(f3 是负对照)。
- `RERANK_TOP_K=0` 安全(trainer.py 有守卫,final test 降为 2 epochs)。
- smart 保持手动分配:`SMART_ENABLE_AUTO_PIPELINE_CONFIG=False`(智慧分配已证伪)。
- **计时实验不要并行跑两个 cell**(GPU 争抢会作废吞吐数字);GPU 争抢与 CUDA_VISIBLE_DEVICES 事故已在 Phase 1 解决。
- serial 参考跑法:`ENABLE_STRATEGIES=["serial"]`。
- run_all.py 顶部配置区:MAX_EVENTS / SEARCH_SPACE / COARSE_TRIALS / RERANK_TOP_K / GPU_LIST / 各策略 worker 数。

## 9. 挂账清单(未完成,新会话从这里接手)

1. **Phase 2 是否跑**(用户决定):cell E = 100K×2GPU(隔离数据/GPU 轴,判 naive-vs-smart 交叉点由谁驱动);cell F = 200K×3GPU(找 naive-vs-DP 交叉点;MAX_EVENTS=200000、COARSE_TRIALS=12、RERANK=0,其余同 D);DP 保真度专项(DP 两规模都选 time_linear+normalize 变体,排查 RL 探索路径分叉)。
2. **摘要 v4 用户批改** + 2 个挂账同步点(scores and selection → selection;among consecutive interactions → among interactions)。
3. **段 5-8 用户批改**。
4. E10 完整实验表(≥4-6 数据集、≥2 模型、3 常驻 baseline、≥2 环境——导师硬性要求)。
5. E11 C3 vs BOHB/Ray Tune;E12 时间分解图(数据在 comparison.json)。
6. A 占位:[ref] 引用 ×9(sample.bib)、DepTGL 全称、X datasets / Y model families。
7. `待清除/` 6 项等用户确认后真删。
8. Overleaf 网页项目(用户自建;需要 acmart.cls)。

## 10. 新会话自检题(答对才说明继承成功)

**Q1**:C2 为什么判定「部分成立」而不是「反超」或「恒输」?naive 打赢了谁、输给了谁?

**Q2**:为什么 08-13 的 DP 1.6× 不能和 Phase 1 的 naive/DP 0.93 并列引用?
