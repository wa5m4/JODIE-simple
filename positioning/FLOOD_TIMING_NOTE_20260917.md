# CPU 洪水期实验标注:分数可信,计时不可信(2026-09-17)

> 用户裁定(2026-09-17):洪水下的测试**分数可信、时间不可信**,明确标注,
> 有机会时重跑计时。

## 洪水窗口

- 2026-09-16 白天起,dongyu 的 yolov13 进程群持续占用 ~27 核(每个 264-543%),
  48 核机器负载 200-235(超订 4.2-4.9×),通宵未退。
- 2026-09-17 上午负载降至 135-165 区间(仍超订 2.8-3.4×),用户裁定**启动全链条**
  (phase5 重启,2026-09-17 10:18,chain_all_p5_restart.log)。
- 负载 < 96(超订 2×)才算非洪水条件;此前所有 run 均属洪水期。

## 数据判读规则

| 数据 | 判读 |
|---|---|
| val/test 分数、选出的架构 | **可信**——完全由种子链决定(专用 Generator、预计算负样本、
每 trial `_set_seed(42+trial_idx)`、确定性评估),与执行时序无关;
金丝雀三臂位级一致已证明时序差异不影响结果 |
| 耗时、加速比 | **照记 + 标 ★ + 事后校验**(2026-09-17 修订):填表前按偏差判据校验,
通过直接入表(★ 保留、脚注注明校验方式),不通过才夜间补跑 |

## 计时校验判据(2026-09-17 修订:不默认补跑)

- 干净参照值(洪水前):B 三臂 serial 10864.3s / 复跑 11930.4s(自然方差 ~10%)、
  naive 9928.8s、naive_async 11266.6s。
- **有同构干净对照的(20K×50t)**:洪水值 vs 干净参照偏差 ≤ **15%**(≈1.5× 复跑方差)→ 直接入表。
  首个校验点 = C-serial(C 与 B 同为 20K×50t,serial 单卡,应 ≈ 10864.3s)。
- **无直接对照的(100K/200K×12t)**:timing_log 每 trial 节奏平稳性(无评估段尖峰/抖动)+
  跨臂加速比自洽(五臂相对关系不因洪水翻转)+ 20K→100K 成本外推三方校验;
  明显异常(>1.5× 期望或 trial 级抖动)→ 夜间补跑。
- 入表后 ★ 保留,脚注写「洪水期计时,已按 X 判据校验(偏差 Y%)」;
  不通过者列入补跑清单(主矩阵完成后统一生成)。
- 链条自带的 pmon 污染裁决只检测 GPU 侧活跃污染(sm>15%),**不覆盖 CPU 洪水**;
  本标注文件是 CPU 侧的补充标注,判读计时时两者都要看。
- **2026-09-18 用户裁定:取消污染自动重跑**——所有 run 只跑一遍;污染(pmon sm>15%)/CPU 洪水
  都在日志与表格标注(⚠/★),不重跑;全部跑完后统一检验,只有误差不可容忍的 run 才补跑。
- 注:历史「慢 12-18×」是负载 228 时 CPU-heavy 策略(smart_sync 8/50 trials 6.7h vs
  干净全量 2.76h)的观测;09-17 负载 168 下 B-dp 实测与干净期逐分钟持平——
  故改为逐 run 校验,不按最坏情况一刀切。

## 相关记录

- B-dp 保真门槛(修复后复测,本次链条第 1 个 run):其**分数**是门槛判据,不受洪水影响;
  其计时同样标注不可信。
- B-smart_sync 部分 run(2026-09-16 13:04-19:53,8/50 trials)已改名
  `cellB_smart_sync_20260916_partial.log`,不计入任何结论。
